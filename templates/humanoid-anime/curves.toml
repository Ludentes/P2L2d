# Post-MLP response curves for humanoid-anime template.
# Applied at inference time, not baked into the model.
# Param names are template-canonical (matching schema.toml).
#
# Unlisted params pass through unchanged (identity curve).

[params.EyeLOpen]
type = "gamma"
gamma = 0.7
lo = 0.0
hi = 1.0

[params.EyeROpen]
type = "gamma"
gamma = 0.7
lo = 0.0
hi = 1.0

[params.EyeLSmile]
type = "gamma"
gamma = 0.8
lo = 0.0
hi = 1.0

[params.EyeRSmile]
type = "gamma"
gamma = 0.8
lo = 0.0
hi = 1.0

[params.MouthOpenY]
type = "gamma"
gamma = 0.6
lo = 0.0
hi = 1.0

[params.MouthForm]
type = "linear_scale"
scale = 1.3
centre = 0.0
lo = -1.0
hi = 1.0

[params.BrowLY]
type = "linear_scale"
scale = 1.2
centre = 0.0
lo = -1.0
hi = 1.0

[params.BrowRY]
type = "linear_scale"
scale = 1.2
centre = 0.0
lo = -1.0
hi = 1.0
